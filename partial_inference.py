def run_partial_inference(node_id: str):
    """
    Perform partial inference up to `node_id`.
    Return some form of result (e.g., numpy array, or string).
    """
    # TODO: real partial inference code with OpenVINO
    return f"Partial inference result at layer/node: {node_id}"
